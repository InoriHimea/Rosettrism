# 2026-06-13 质量评分提升到 9 分

## 背景

- 当前复核评分约为 8.4，主要短板集中在真实多语言歌词回归覆盖、歌词播放前端可维护性、以及交付现场整洁度。
- 本计划对应 `requirement.md` 的 `v2.1.0 - 2026-06-13 - 质量评分提升到 9 分`。
- 常规自动化测试使用固定 fixture；真实 Provider 数据通过可选 live capture 脚本采集到忽略目录，避免 CI 受网络、版权接口、限流影响。

## 目标

- [x] 建立本轮质量提升需求与计划记录。
- [x] 拆出歌词播放复杂纯逻辑，并补充 Node 单测。
- [x] 增加国语、粤语、日语、英语四语种 Playwright 回归。
- [x] 提供真实 Provider 数据采集脚本，用于发布前刷新证据。
- [x] 完成必跑验证并记录结果。

## 非目标

- 不新增 Provider，不改变聚合输出协议。
- 不全量修复历史乱码文档或历史验证脚本，只保证本轮新增内容 UTF-8 可读。
- 不把整首真实歌词固化进仓库；fixture 使用诊断片段承载结构化行为。

## 阶段列表

### Phase 1 - 文档与基线审计

#### Task checklist

- [x] 确认当前工作树存在既有 Playwright 生成物和未跟踪验证目录。
- [x] 追加 `requirement.md` v2.1.0 条目。
- [x] 新增本计划文件。
- [x] 确认 `frontend/verification/` 是本轮开始前已有的未跟踪证据目录，本轮保留但不纳入新增交付范围。

#### 验收条件

- [x] 新增文档为 UTF-8 中文可读。
- [x] 明确 `frontend/test-results/` 与 live capture 输出不进入提交。

### Phase 2 - 多语言 fixture 与自动化测试

#### Task checklist

- [x] 新增四语种 fixture：国语《龙战骑士》、粤语《海阔天空》、日语《ブルーバード》、英语《Just One Last Dance》。
- [x] 新增 Playwright 多语言播放回归。
- [x] 新增 `verify:multilingual-live` 脚本，用真实 Provider 拉取发布前证据。

#### 验收条件

- [x] 常规 Playwright 不依赖外部网络。
- [x] 四语种场景验证播放视图、逐字节点、注解/读音/假名、移动端横向溢出。

### Phase 3 - 歌词播放可维护性重构

#### Task checklist

- [x] 新增 `frontend/src/lyricPlaybackViewModel.js`。
- [x] 将 meta line、倒计时、lane 分配、annotation label、进度计算等纯逻辑从 `LyricPlaybackView.jsx` 迁出。
- [x] 新增 `frontend/src/__tests__/lyricPlaybackViewModel.test.js`。

#### 验收条件

- [x] `LyricPlaybackView.jsx` 保持渲染职责，复杂行为可由 Node 单测直接覆盖。
- [x] 单测覆盖标题/艺人 fallback、meta 去重、倒计时、双行 lane、注解标签优先级、逐字进度。

### Phase 4 - 验证、评分复核、发布准备

#### Task checklist

- [x] `cargo fmt --check`
- [x] `cargo test --no-fail-fast`
- [x] `npm run build`
- [x] `npm run test:unit`
- [x] `npm test`
- [x] `git diff --check`
- [x] 清理无意生成物，确认工作树只留下本轮有意改动和用户既有现场。

#### 评分目标

- 后端/API/缓存维持 8.8+。
- 前端歌词播放提升到 8.8+。
- 测试质量提升到 9.0。
- 可维护性提升到 8.6+。
- 整体评分达到 9.0+。

## 整体验收条件

- [x] 所有必跑验证通过，或有明确环境阻断说明。
- [x] 新增文档、fixture、脚本、测试均纳入仓库原生工作流。
- [x] `frontend/test-results/` 与 `frontend/verification/live-multilingual/` 不进入提交。

## 整体测试记录

| 日期 | 命令 / 检查 | 结果 | 备注 |
|------|-------------|------|------|
| 2026-06-14 | `cargo fmt --check` | 通过 | 无格式差异 |
| 2026-06-14 | `cargo test --no-fail-fast` | 通过 | 147 个 lib 测试、14 个 CLI 测试、fixture/schema 测试通过；仅有 Windows 硬链接缓存 warning |
| 2026-06-14 | `npm run build` | 通过 | dist 已重建，bundle hash 更新 |
| 2026-06-14 | `npm run test:unit` | 通过 | 10 个 Node 单测通过 |
| 2026-06-14 | `npm test` | 通过 | 7 个 Playwright 测试通过，含四语种回归 |
| 2026-06-14 | `git diff --check` | 通过 | 仅 CRLF 提示，无 whitespace error |

## 整体完成状态

- [ ] 未开始
- [ ] 进行中
- [x] 已完成
- [ ] 已取消
